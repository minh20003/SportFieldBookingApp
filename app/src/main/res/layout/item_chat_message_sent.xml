<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="end"
    android:orientation="vertical"
    android:paddingStart="60dp"
    android:paddingEnd="8dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <!-- Message Bubble -->
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_message_sent_bubble"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:text="Tin nhắn của tôi"
            android:textColor="@android:color/white"
            android:textSize="15sp" />

        <!-- Sending Indicator -->
        <ProgressBar
            android:id="@+id/progressSending"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center_vertical|end"
            android:layout_marginEnd="4dp"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Status Row -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="10:30"
            android:textColor="@color/text_hint"
            android:textSize="11sp" />

        <!-- Status Icon: Sending -->
        <ImageView
            android:id="@+id/ivStatusSending"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_clock"
            app:tint="@color/text_hint"
            android:visibility="gone" />

        <!-- Status Icon: Sent (single check) -->
        <ImageView
            android:id="@+id/ivStatusSent"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_check_single"
            app:tint="@color/text_hint"
            android:visibility="gone" />

        <!-- Status Icon: Delivered (double check gray) -->
        <ImageView
            android:id="@+id/ivStatusDelivered"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_check_double"
            app:tint="@color/text_hint"
            android:visibility="gone" />

        <!-- Status Icon: Seen (double check blue) -->
        <ImageView
            android:id="@+id/ivStatusSeen"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_check_double"
            app:tint="@color/primary"
            android:visibility="gone" />

        <!-- Status Icon: Failed -->
        <ImageView
            android:id="@+id/ivStatusFailed"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_error"
            app:tint="@color/error"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Retry Button (for failed messages) -->
    <TextView
        android:id="@+id/tvRetry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="?attr/selectableItemBackground"
        android:padding="4dp"
        android:text="Gửi lại"
        android:textColor="@color/error"
        android:textSize="12sp"
        android:visibility="gone" />

</LinearLayout>
